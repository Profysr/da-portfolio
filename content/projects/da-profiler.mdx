---
title: "Da Profiler"
description: "Open-source Python REST API profiler & N+1 query workbench"
date: "2024-04-15"
category: "Open Source"
industry: "Developer Tools"
access: "Open Source"
strategies: ["AST-based Query Analysis", "Flame Graph Visualization", "Middleware Integration"]
tech: ["Python", "FastAPI", "SQLAlchemy", "React"]
github: "https://github.com"
live: "https://github.com"
thumbnail: "/tools/python.svg"
---

## The Problem

N+1 queries kill API performance. Existing profilers are either too heavy (PySpy) or too shallow (Django Debug Toolbar).

## Solution

A lightweight middleware that:

1. **Intercepts** every ORM query via SQLAlchemy event hooks
2. **Analyzes** query patterns using AST parsing — detects N+1, missing indexes, cartesian products
3. **Visualizes** results as interactive flame graphs in a React dashboard
4. **Integrates** with FastAPI/Starlette/Django via one-line middleware

## Usage

```python
from da_profiler import ProfilerMiddleware

app.add_middleware(ProfilerMiddleware, sample_rate=0.1)
```

Visit `/profiler` to see live query analysis.

## Changelog

### v0.5.0 - 2024-12-01
- Added Django ORM support
- Flame graph export as SVG/PNG
- Query deduplication algorithm

### v0.4.0 - 2024-09-15
- React dashboard with real-time updates
- N+1 detection with severity scoring
- FastAPI middleware integration

### v0.3.0 - 2024-06-20
- SQLAlchemy event hook interception
- AST-based query pattern analysis
- Basic CLI profiler

### v0.2.0 - 2024-04-15
- Initial proof of concept
- Query capture middleware
- N+1 detection prototype